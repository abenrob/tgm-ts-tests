import { TargomoClient } from './targomoClient';
import { StatisticsGroupId, BenchmarkCriteria, BoundingBox } from '../index';
/**
 * @Topic Benchmarks
 */
export declare class BenchmarksClient {
    private client;
    constructor(client: TargomoClient);
    /**
     *
     */
    fetch(group: StatisticsGroupId, conditions: BenchmarkCriteria[], bounds: BoundingBox): Promise<any>;
    /**
     *
     */
    metadata(key: StatisticsGroupId): Promise<any[]>;
}
