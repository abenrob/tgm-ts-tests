import { TargomoClient } from './targomoClient';
import { LatLng } from '../index';
export declare class GeocodePhotonClient {
    private client;
    constructor(client: TargomoClient);
    geocode(query: string, center?: LatLng, language?: string): Promise<any[]>;
    private buildPlaceDescription;
}
