import { TargomoClient } from './targomoClient';
import { StatisticsGroupId, SimilarityCriteria, BoundingBox } from '../index';
/**
 * @Topic Similarity
 */
export declare class SimilarityClient {
    private client;
    constructor(client: TargomoClient);
    /**
     *
     */
    metadata(key: StatisticsGroupId): Promise<any[]>;
    /**
     *
     */
    fetch(group: StatisticsGroupId, cell: number, conditions: SimilarityCriteria[], bounds: BoundingBox, normalizeOnViewport: boolean): Promise<any>;
}
